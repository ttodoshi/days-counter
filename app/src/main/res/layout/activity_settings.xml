<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey"
    android:orientation="vertical"
    tools:context="org.todoshis.dayscounter.activities.Settings">

    <ScrollView
        android:id="@+id/scrollContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true">

        <LinearLayout
            android:id="@+id/linearLayoutForButtons"
            android:layout_width="300sp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:orientation="vertical">

            <Button
                android:id="@+id/addCounter"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="35sp"
                android:background="@drawable/button_layout"
                android:onClick="addCounter"
                android:text="@string/create_counter"
                android:textColor="@color/white" />

            <Button
                android:id="@+id/changeStartDate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="35sp"
                android:layout_marginBottom="35sp"
                android:background="@drawable/button_layout"
                android:onClick="showCalendarAlert"
                android:text="@string/change_date"
                android:textColor="@color/white" />

            <Button
                android:id="@+id/changePhrase"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="35sp"
                android:layout_marginBottom="35sp"
                android:background="@drawable/button_layout"
                android:onClick="showPhraseAlert"
                android:text="@string/change_phrase"
                android:textColor="@color/white" />

            <Button
                android:id="@+id/deleteLastCounter"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="35sp"
                android:background="@drawable/button_layout"
                android:onClick="deleteLastCounter"
                android:text="@string/del_last_counter"
                android:textColor="@color/white" />

        </LinearLayout>
    </ScrollView>

</RelativeLayout>